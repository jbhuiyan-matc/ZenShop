{
  "name": "zenshop",
  "version": "0.1.0",
  "private": true,
  "description": "ZenShop - Secure E-Commerce Platform",
  "scripts": {
    "build": "cd frontend && npm install && VITE_API_URL=/api npm run build && cd ../backend && npm install && npm run build",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=18"
  }
}
